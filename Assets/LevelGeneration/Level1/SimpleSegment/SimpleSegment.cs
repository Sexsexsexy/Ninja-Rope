using UnityEngine;
using System.Collections;

public class SimpleSegment : Segment {

	public override void Randomize()
	{
//		beginningJoint.transform.localPosition = Vector3.zero;
//		float distance = Random.Range(3, 5) + beginningJoint.Box.bounds.extents.x;
//		endJoint.transform.localPosition = Vector3.right * distance;
//		endJoint.PlayerTrigger = true;
	}
}
